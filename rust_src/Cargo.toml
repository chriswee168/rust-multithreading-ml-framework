[package]
name = "rust_src"
version = "0.1.0"
edition = "2024"

# Configure rust project as shared library.
[lib]
name = "ai_core"
crate-type = ["cdylib", "rlib"]

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
rand = "0.8.5"