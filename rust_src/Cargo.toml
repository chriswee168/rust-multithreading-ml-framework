[package]
name = "rust_src"
version = "0.1.0"
edition = "2024"

# Configure rust project as shared library.
[lib]
name = "libai_core"
crate-type = ["cdylib", "rlib"]

[dependencies]
serde = "1.0.228"
rand = "0.8.5"